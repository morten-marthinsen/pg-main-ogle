# 0.3 — Package Loader

**Skill:** LP-00-brief-classifier
**Position:** Layer 0, Microskill 3
**Purpose:** Load all available CopywritingEngine output packages for the project (Downstream Mode only). Produce a complete inventory of found packages with paths, sizes, and freshness so Layer 3 extraction microskills know exactly what to consume.
**Effort Level:** low
**Dependencies:** `mode-declaration.md` (must show `operating_mode: downstream`), project folder path
**Output:** `package-inventory.md`

---

## Input Schema

```yaml
input:
  project_folder: "[path to project folder]"
  product_name: "[product name]"
  operating_mode: "downstream"  # This microskill ONLY runs in Downstream Mode
  packages_found: "[list from mode-declaration.md]"
```

---

## Execution Logic

**Step 1:** Confirm operating mode is `downstream`. If `standalone`, skip this microskill entirely and log: "0.3 skipped — Standalone Mode."

**Step 2:** For each standard CopywritingEngine package type, search the project folder and load metadata:

```
Package types to scan:
  01-research/outputs/          → FINAL_HANDOFF.md, research-quotes.md, any .md/.json
  03-big-idea/outputs/          → big-idea-output.json
  04-headline/outputs/          → headline-package.json
  05-promise/outputs/           → promise-output.json
  06-root-cause-mechanism/outputs/ → mechanism-package.json
  07-offer/outputs/             → offer-package.json
  09-campaign-brief/outputs/    → campaign-brief.json
  19-campaign-assembly/outputs/ → assembled-draft.md
  20-editorial/outputs/         → editorial-package.md
```

**Step 3:** For each found package, record:
- Package name
- Full file path
- File size (bytes)
- Last modified date
- Freshness status: `fresh` (modified within 7 days), `recent` (7-30 days), `stale` (>30 days)

**Step 4:** For each missing package, record as `not_found`.

**Step 5:** Calculate package coverage: `[found packages] / [total standard packages]` as a percentage. Note which Layer 3 extractions will be possible vs. impossible based on what's available.

---

## Output Schema

`package-inventory.md`:

```markdown
# Package Inventory — [Product Name]

## Operating Mode: DOWNSTREAM
## Package Coverage: [X/9] ([XX%])

## Available Packages

### [Package Name]
- Path: [absolute path]
- Size: [bytes / KB]
- Last Modified: [date]
- Freshness: [fresh | recent | stale]
- Layer 3 Consumer: [which extraction microskill will use this]

[Repeat for each found package]

## Missing Packages
- [package name]: not_found — Impact: [what Layer 3 extraction cannot run]
- [package name]: not_found — Impact: [what Layer 3 extraction cannot run]

## Layer 3 Extraction Forecast
| Extraction Microskill | Required Packages | Available? | Can Execute? |
|-----------------------|-------------------|-----------|-------------|
| 3.1: Research Intelligence | 01-research outputs | [Y/N] | [Y/N] |
| 3.2: Copy Package Distiller | 03-big-idea, 04-headline, 05-promise, 06-mechanism | [partial/Y/N] | [Y/N] |
| 3.3: Offer Package Extractor | 07-offer | [Y/N] | [Y/N] |

## Staleness Warnings
[List any packages marked 'stale' — these may contain outdated data]
```

---

## Quality Criteria
- Only executes when operating mode is `downstream`
- All 9 standard package locations scanned
- Each found package has path, size, date, and freshness recorded
- Missing packages explicitly listed with downstream impact
- Layer 3 extraction forecast accurately reflects what's possible
- File exists after execution

## Failure Modes
- Operating mode is `standalone` → Skip entirely, log skip reason
- Project folder doesn't exist → HALT with clear error
- Packages found but files are empty (0 bytes) → List as found but flag `empty_file` warning
- Package files exist but are malformed JSON → List as found, flag `parse_warning` for Layer 3
