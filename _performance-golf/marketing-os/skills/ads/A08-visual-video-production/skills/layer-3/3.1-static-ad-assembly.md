# 3.1: Static Ad Assembly

## Purpose
Combine hero images + text overlays + CTA graphics + logos into complete static ad units per platform per variant. Only assets with PASS verdict from Layer 2.5 enter assembly. Produces ready-to-deploy static ad creatives.

## Inputs
- PASS images from Layer 2.5 quality review
- Text overlay specs from Layer 2.6
- CTA graphics specs (from A07 CTA copy + A05 visual direction)
- Logo assets (from project/brand assets)
- Platform specs (aspect ratios, safe zones)
- Assembly tool availability (Canva API, FFmpeg, or human designer brief)

## Process
```
FOR EACH static ad variant:

  INPUT:
    - Hero image (PASS from Layer 2.5)
    - Text overlay spec (from Layer 2.6)
    - CTA graphic spec (from A07 CTA copy)
    - Logo (from project assets)
    - Platform target (from A03)

  ASSEMBLY STEPS:
    1. Load hero image at correct aspect ratio
    2. Apply text overlay:
       - Headline text (from A07) at specified position
       - Font, size, color from A05 visual direction
       - Ensure text contrast against background (readability check)
       - Ensure text is within safe zone (not cropped on platform)
    3. Apply CTA element:
       - CTA text (from A07)
       - CTA button/banner style (from A05)
       - Position per platform convention
    4. Apply logo:
       - Position per brand guidelines
       - Size proportional to ad dimensions
    5. Export:
       - JPG for photography-based images
       - PNG for graphics/text-heavy images
       - Resolution per platform spec table

  ASSEMBLY TOOL:
    Primary: Canva API (template-based, reliable text rendering)
    Fallback: FFmpeg (overlay compositing)
    Fallback 2: Output assembly spec for human designer

  QUALITY CHECK PER ASSEMBLED STATIC:
    - Text readable at mobile preview size: [PASS/FAIL]
    - CTA visible and unobstructed: [PASS/FAIL]
    - Overall composition balanced: [1-10]
    - Brand consistency (logo, colors): [PASS/FAIL]
    - Platform spec compliance (size, format, file size): [PASS/FAIL]
```

## Output Schema
```markdown
# 3.1: Static Ad Assembly
## Execution Context
- Skill: A08 -- Visual/Video Production
- Layer: 3
- Timestamp: [execution time]
- Input files read: [PASS images, 2.6 text overlay specs, brand assets]
- Model used: sonnet
- Tools used: [Canva API / FFmpeg / human spec]

## Output
### Assembly Summary
- Total static ads to assemble: [count]
- Static ads assembled successfully: [count]
- Static ads requiring human assembly: [count]

### Per-Ad Assembly Log
#### Static Ad: C01-V01-meta-feed-complete
- Platform: Meta Feed (1:1, 1080x1080)
- Components:
  - Hero image: [file path] -- PASS from 2.5.1
  - Text overlay: [headline text, font, position]
  - CTA: [CTA text, button style, position]
  - Logo: [position, size]
- Assembly tool: [Canva/FFmpeg/human spec]
- Quality check:
  | Criterion | Result |
  |-----------|--------|
  | Text readable at mobile | [PASS/FAIL] |
  | CTA visible | [PASS/FAIL] |
  | Composition balanced | [1-10] |
  | Brand consistent | [PASS/FAIL] |
  | Platform spec | [PASS/FAIL] |
- Exported file: [path with naming convention]
- Export format: [JPG/PNG]
- File size: [KB]

[Repeat for each static ad]

### Assembly Tool Usage
| Tool | Count | Success Rate |
|------|-------|-------------|
| Canva API | [n] | [%] |
| FFmpeg | [n] | [%] |
| Human spec | [n] | N/A |

## Quality Metrics
- All static ads attempted: [Y/N]
- All text overlays readable at mobile size: [Y/N]
- All CTAs visible and unobstructed: [Y/N]
- All platform specs met: [Y/N]
- No FAIL/FALLBACK assets used in assembly: [Y/N]
- Schema compliance: [Y/N]
- Minimum thresholds met: [Y/N]
```

## Output File
- **Path:** `layer-3-outputs/3.1-static-assembly.md`
- **Minimum size:** 3KB

## Quality Checks
- Only PASS assets from Layer 2.5 used in assembly (no FAIL assets)
- Text is readable at mobile preview size (not just desktop)
- CTA is visible and not obscured by other elements
- Text positions respect platform safe zones
- Logo placement follows brand guidelines
- Export format and resolution match platform specs
- Naming convention applied to all exported files

## Dependencies
- Layer 2.5 must be complete (GATE_2.5_COMPLETE.yaml exists)
- All component assets must have PASS verdict
- 2.6 Text Overlay Specifications must exist
- Assembly tool must be available
