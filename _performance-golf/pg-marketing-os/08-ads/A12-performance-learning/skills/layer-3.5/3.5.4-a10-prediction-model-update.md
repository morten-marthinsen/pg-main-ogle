# 3.5.4: A10 Prediction Model Update

**Version:** 1.0
**Layer:** 3.5 (Learning Propagation)
**Type:** Engine Propagation
**Model:** opus
**Dependencies:** Layer 3 outputs (GATE_3 must be PASS), 2.3 scoring-model-audit, 3.5 prediction-learnings
**Output:** layer-3.5-outputs/3.5.4-a10-prediction-model-update.md

---

## Purpose

Update A10's prediction model based on the scoring model audit (2.3) and prediction learnings (3.5). Specific updates: adjust scoring formula weights, add new predictive signals identified in Layer 3, update benchmark reference data with actual campaign data, adjust platform-specific scoring adjustments, update creative fatigue predictions based on actual fatigue curves. Write A10-PREDICTION-PATCH.md documenting all proposed model changes. POST-CAMPAIGN mode only.

---

## Inputs

```yaml
input:
  from_2.3:
    scoring_model_audit: "layer-2-outputs/2.3-scoring-model-audit.md"
    # Contains: per-criterion correlations, weight adjustment recommendations
  from_3.5_learnings:
    prediction_learnings: "layer-3-outputs/3.5-prediction-learnings.md"
    # Contains: systematic biases, missing signals, specific adjustments
  from_3.4:
    fatigue_learnings: "layer-3-outputs/3.4-fatigue-learnings.md"
    # Contains: actual fatigue data for updating fatigue predictions
  from_3.6:
    validated_learnings: "layer-3-outputs/3.6-learning-validator.md"
    # Contains: all learnings targeting A10
  current_a10:
    scoring_model: "[A10 scoring configuration if documented]"
```

---

## Process

### Step 1: Verify Post-Campaign Mode and Gate 3

```
IF mode == MID_FLIGHT:
  --> SKIP -- produce deferral stub
```

### Step 2: Compile All A10 Model Changes

```
FROM 2.3 scoring model audit AND 3.5 prediction learnings:

  (a) SCORING FORMULA WEIGHT ADJUSTMENTS:
    For each scoring criterion:
      criterion: [name]
      current_weight: [X]
      recommended_weight: [Y]
      change: [+/-Z]
      correlation_with_CPA: [r]
      correlation_with_hook_rate: [r]
      data_basis: "[X] variants, [Y] confidence"
      risk_assessment: "LOW/MEDIUM/HIGH risk of over-fitting to single campaign"

  (b) SYSTEMATIC BIAS CORRECTIONS:
    For each identified bias:
      bias_type: [format/visual/hook/platform]
      current_bias: "[description -- e.g., overestimates video by 0.8 points]"
      proposed_correction: "[specific adjustment -- e.g., reduce video bonus from +1.0 to +0.5]"
      data_basis: "[evidence]"

  (c) NEW PREDICTIVE SIGNALS:
    For each proposed new signal:
      signal_name: [name]
      description: [what it measures]
      correlation_with_performance: [r value]
      proposed_starting_weight: [X]
      implementation_notes: "[how A10 should calculate this signal]"

  (d) PLATFORM-SPECIFIC SCORING ADJUSTMENTS:
    For each platform:
      platform: [name]
      current_modifier: [X]
      proposed_modifier: [Y]
      data_basis: "[platform-specific performance patterns]"

  (e) BENCHMARK REFERENCE DATA UPDATES:
    campaign_data_to_add:
      vertical: [vertical]
      campaign_CPA: $[X]
      campaign_ROAS: [X]x
      campaign_hook_rate: [X]%
      win_rate: [X]%
      variants_tested: [X]
    NOTE: This data should be ADDED to existing benchmarks, not replace them.
    Running average across campaigns provides better reference data.

  (f) CREATIVE FATIGUE PREDICTION UPDATES (from 3.4):
    For each relevant data point:
      vertical_half_life_benchmark: current=[X] days, actual=[Y] days, proposed=[Z] days
      UGC_fatigue_rate: [data from 3.4]
      polished_fatigue_rate: [data from 3.4]
      frequency_fatigue_threshold: current=[X], actual=[Y], proposed=[Z]
```

### Step 3: Write A10-PREDICTION-PATCH.md

```
PRODUCE patch file:

  # A10-PREDICTION-PATCH.md
  ## Patch Metadata
  - Source: A12 Performance Learning Loop -- [project name]
  - Campaign: [dates], [spend], [variants]
  - Overall prediction accuracy: [X]%
  - Status: PROPOSED

  ## 1. Scoring Weight Adjustments
  | Criterion | Current | Proposed | Change | Correlation | Confidence | Risk |
  |-----------|---------|----------|--------|-------------|------------|------|

  ## 2. Bias Corrections
  | Bias Type | Current Effect | Correction | Data Basis |

  ## 3. New Predictive Signals
  | Signal | Description | Correlation | Proposed Weight | Implementation |

  ## 4. Platform Scoring Adjustments
  | Platform | Current Modifier | Proposed | Data Basis |

  ## 5. Benchmark Reference Data
  - Add campaign data to running benchmarks: [specific values]

  ## 6. Fatigue Prediction Updates
  | Parameter | Current | Actual | Proposed | Basis |

  ## Application Instructions
  [Exact file paths, sections, current values, proposed values for each change]

  ## Cumulative Model Improvement Tracking
  - A10 accuracy before this campaign: [X]% (or "first calibration")
  - Expected accuracy improvement: [estimate based on changes]
  - This is calibration campaign #[N] for the model

  ## Approval Required
  [ ] Human has reviewed all scoring weight changes
  [ ] Human has reviewed bias corrections
  [ ] Human has reviewed new signals
  [ ] Human has reviewed platform adjustments
  [ ] Human has reviewed benchmark data additions
  [ ] Human has reviewed fatigue prediction updates
```

---

## Output Schema

```yaml
output:
  a10_prediction_model_update:
    execution_status: enum[COMPLETE, DEFERRED_MID_FLIGHT]
    patch_file: "A10-PREDICTION-PATCH.md"
    changes:
      weight_adjustments: integer
      bias_corrections: integer
      new_signals: integer
      platform_adjustments: integer
      benchmark_updates: integer
      fatigue_updates: integer
      total_changes: integer
    model_improvement:
      previous_accuracy: float
      expected_improvement: string
      calibration_campaign_number: integer
    approval_status: enum[PROPOSED]
```

---

## Output File

- **Path:** `layer-3.5-outputs/3.5.4-a10-prediction-model-update.md`
- **Minimum size:** 3KB

---

## Quality Checks

1. All weight adjustments include specific magnitude, correlation data, and risk assessment
2. Bias corrections address all systematic biases identified in Layer 2
3. New signals have correlation evidence and proposed starting weights
4. Platform adjustments backed by platform-specific data
5. Benchmark reference data ADDS to existing (does not replace)
6. Fatigue prediction updates use actual campaign fatigue data
7. Cumulative improvement tracked (this is campaign #N)
8. All changes PROPOSED only -- no direct engine file edits

---

## Dependencies

- GATE_3 must be PASS
- 2.3 scoring-model-audit output
- 3.5 prediction-learnings output
- 3.4 fatigue-learnings output
- Current A10 model configuration
- POST_CAMPAIGN mode only
- Runs in parallel with 3.5.1-3.5.3, 3.5.5
