
Each token is influenced by the prompt (or in my case skill) context and then also the token that came just before it. 

My question is this: if only the token just before the new token being generated is considered (and not the collection of tokens, ie the entire body of writing) -- how is the AI able to make a complex argument that takes into account everything that has been said just before the generation? 

Unless my understanding of tokens are inaccurate and a "token" is more than just a fraction of a word and contains vector connections to other (including prior) tokens in the space -- this would seem to be a problem. Can you clarify? 





Re: fine tuning, I would like to understand this better. Also, given my future model is going to be adapting the copywriting engine (or major parts of it) to each client, then fine tuning based on data from ads, page engagement (time spent near certain text/elements, heatmap, etc), page conversion and so on.  I am asking about this in the ultimate or ideal sense, not for now. In other words, given no technology or personnel limits, and given lots of test data on outputs -- then I imagine part of our competitive advantage could be to create extremely sophisticated and fine tuned model that produces world class copy on demand for each client 

One additional question about fine tuning and iteration in general. If you have the output (model + my system) but then you add human curation layer in order to make initial outputs strong enough to test, then technically the data is not purely reflecting the model/system output -- it's reflecting the human output more (though this depends on divergence of human edit)