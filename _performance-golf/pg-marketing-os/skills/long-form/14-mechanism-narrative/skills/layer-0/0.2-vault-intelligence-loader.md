# Layer 0.2 — Vault Intelligence Loader

**Version:** 1.0 (V3)
**Layer:** 0 (Foundation)
**Type:** Data Loading
**Dependencies:** mechanism-narrative-vault-intelligence.json
**Output:** vault_patterns loaded into skill context

---

## Purpose

Load pre-computed V3 vault intelligence for the mechanism-narrative domain. This file contains verbatim specimens, frequency patterns, and aggregate statistics extracted from 261 elite Tier 1 copywriting pieces.

---

## V3 Vault Intelligence Source

**Primary Source:** `vault-intelligence/mechanism-narrative-vault-intelligence.json`
**Version:** v3 (built 2026-01-27)
**Coverage:** 261 V3 extraction files from Tier 1 elite swipe vault
**Format:** JSON with `specimens`, `patterns`, and `statistics` sections

---

## Loading Protocol

### Step 1: Load V3 Vault Intelligence File

```
LOCATE: vault-intelligence/mechanism-narrative-vault-intelligence.json

VALIDATE:
  - File exists and is valid JSON
  - Contains "version": "v3"
  - Contains "specimens" section with non-empty content
  - Contains "patterns" section with frequency distributions

IF file missing or invalid:
  SET status = FAIL
  LOG "V3 vault-intelligence build must run first"
  HALT
```

### Step 2: Extract Specimens

```
FROM specimens section:
  LOAD all verbatim content specimens
  INDEX by category/type for downstream lookup
  VERIFY verbatim_quote/text fields are present (not summaries)
```

### Step 3: Extract Patterns

```
FROM patterns section:
  LOAD all frequency distributions
  IDENTIFY top-frequency categories for defaults
  CALCULATE niche-specific pattern weightings
```

### Step 4: Extract Statistics

```
FROM statistics section:
  LOAD aggregate metrics
  SET quality baselines for validation gates
```

### Step 5: Specimen Tiering

```
FOR each specimen in loaded data:

  CALCULATE narrative_richness_score:
    +2 IF verbatim_explanation present AND length > 30 words
    +2 IF simplification_technique identified (named technique, not generic)
    +1 IF visual_language present with 2+ entries
    +1 IF mechanism_name present and non-empty
    +1 IF explanation complete (technique + verbatim + name all present)
    +1 IF sensory detail present in visual_language (concrete imagery)
    MAX_SCORE: 8

  SORT specimens by narrative_richness_score DESC
  ASSIGN tier:
    Gold = top 10% (narrative_richness_score >= 7)
    Silver = next 15% (narrative_richness_score >= 5)
    Standard = remainder

  TAG each specimen with:
    tier: enum[gold, silver, standard]
    narrative_richness_score: integer (0-8)

  OUTPUT tier_distribution:
    total_specimens: integer
    gold_count: integer
    silver_count: integer
    standard_count: integer
```

### Step 6: Type-Matched Retrieval

```
DEFINE match_fields:
  primary: simplification_technique (e.g., analogy, metaphor, step_by_step, contrast)
  secondary: niche (e.g., health, finance, survival, self-improvement)

WHEN campaign_context is available:
  STRATEGY 1 — exact_match:
    FILTER specimens WHERE simplification_technique == target AND niche == target_niche
    PRIORITIZE Gold tier first, then Silver

  STRATEGY 2 — technique_match:
    FILTER specimens WHERE simplification_technique == target (any niche)
    PRIORITIZE Gold tier, same-niche Silver, then cross-niche Silver

  STRATEGY 3 — cross_technique:
    RETURN all Gold specimens across simplification techniques
    INCLUDE Silver specimens from target niche

WHEN campaign_context is NOT available:
  RETURN full specimen set with tier metadata attached
  SORT by narrative_richness_score DESC

OUTPUT retrieval_result:
  matched_specimens: [specimen]
  match_strategy_used: enum[exact_match, technique_match, cross_technique, full_set]
  gold_specimens_available: integer
  silver_specimens_available: integer
```

---

## Output

Pass loaded vault intelligence to all downstream microskills as `vault_mechanism-narrative_intelligence`.

Additionally pass tiered + matched specimens to:
- **0.2.5-specimen-decomposer.md** (Gold specimens for full decomposition, Silver for template-only decomposition)

---

## Quality Gates

- [ ] V3 JSON file loads successfully
- [ ] Specimens contain verbatim content (not summaries)
- [ ] All pattern categories populated
- [ ] Coverage percentage > 85%
- [ ] Loading status = complete
- [ ] Tiering applied: Gold specimens identified (top 10%)
- [ ] Tiering applied: Silver specimens identified (top 25%)
- [ ] Type-matched retrieval functional with campaign context
- [ ] Fallback to full set works without campaign context
- [ ] Tier distribution logged
