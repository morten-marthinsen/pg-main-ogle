# Layer 0.1 — Upstream Package Loader

## Purpose

Load and parse all required upstream skill packages (mechanism, structure, promise, root cause, proof inventory, headline) into working memory for lead engineering. Extract the specific fields needed for the lead — the elevator pitch elements, the campaign thesis, the emotional frame, and the credibility assets.

---

## Input Requirements

```yaml
upstream_paths:
  mechanism_package: "04-mechanism/outputs/mechanism-package.json"
  structure_package: "08-structure/outputs/structure-package.json"
  promise_package: "05-promise/outputs/promise-package.json"
  root_cause_package: "03-root-cause/outputs/root-cause-package.json"
  proof_inventory: "02-proof-inventory/outputs/proof-inventory-output.json"
  headline: "user_provided OR upstream headline skill output"
  campaign_brief: "09-campaign-brief/outputs/campaign-brief-package.json"
  context_reservoir: "outputs/[project-code]/context-reservoir.md"
```

---

## Execution Protocol

### Step 1: Load Mechanism Package

Extract for lead use:
- `mechanism.name` — the named mechanism for the elevator pitch
- `mechanism.type` — determines which E5 elements are easiest to demonstrate
- `mechanism.explanation_text` — reference only (NOT for the lead — this goes in the argument)
- `mechanism.e5_mechanism_type` — E1-E5 classification for novelty framing
- `mechanism.uniqueness_claim` — the "new and different" element
- `mechanism.proof_elements` — credibility anchors for brief insertion

### Step 2: Load Structure Package

Extract for lead setup:
- `campaign_thesis.statement` — what the campaign argument will prove (lead previews this)
- `argument_strategy.type` — prosecution/revelation/education/demonstration (affects lead tone)
- `flow_architecture.simple_segue` — what the lead must transition INTO
- `downstream_handoffs.for_lead_writing` — specific data the structure skill prepared for the lead
  - `thesis_statement`
  - `primary_promise_echo`
  - `emotional_entry_point`
  - `first_chunk_preview`

### Step 3: Load Promise Package

Extract for elevator pitch:
- `primary_promise.statement` — the BIG promise for the lead
- `primary_promise.emotional_frame` — the emotional context
- `supporting_promises` — additional benefits for the elevator pitch
- `promise_ceiling` — maximum defensible promise level

### Step 4: Load Root Cause Package

Extract for counter-intuitive information:
- `root_cause.expression` — the root cause statement
- `root_cause.three_part_structure.what_they_think` — for counter-intuitive teasing
- `root_cause.three_part_structure.what_it_really_is` — for "surprising" info hint
- `root_cause.villain_profile` — for problem callout framing (if villain exists)

### Step 5: Load Proof Inventory

Extract for credibility insertion:
- `summary.overall_strength` — determines credibility confidence level
- `rankings.knockout_proof` — potential credibility anchor for brief insertion
- Top authority/credential proof elements — for lead credibility insertion

### Step 6: Load Headline

Extract:
- `main_headline` — the lead must continue the headline's emotional momentum
- `subheadline` (if present) — additional context
- Headline's implied promise and mechanism reference

### Step 7: Load Campaign Brief

Extract strategic anchors:
- `campaign_thesis` — the central argument the campaign makes
- `tone_direction` — clinical/emotional/urgent/conversational
- `avatar_summary` — who we're writing for (key demographics, psychographics)
- `market_sophistication` — Schwartz stage (1-5), affects lead approach
- `proof_approach` — how proof should be deployed
- `promise_ceiling` — maximum defensible promise level

**Why this matters for lead:** The lead sets the tone for the entire campaign. Without the brief's strategic anchors, the lead may drift from the campaign's intended positioning, sophistication calibration, or tonal direction.

### Step 8: Load Context Reservoir (if available)

If a context reservoir exists for this project, load it for enriched copy generation:
- `voc_quotes` — 25-40 voice-of-customer quotes for authentic language
- `proof_elements` — top 10-15 proof elements with FULL details
- `customer_language` — how the audience describes their problem and desires
- `fssit_intelligence` — top FSSIT candidates with resonance data
- `expectation_schema` — what the audience expects (and where surprise exists)
- `expression_anchoring` — which phrases penetrate with this audience
- `arena_intelligence` — why the mechanism, root cause, and big idea won
- `counter_intuitive_core` — the central surprising truth
- `campaign_brief_anchors` — strategic decisions every section must honor

**Note:** The context reservoir is OPTIONAL but strongly recommended. It preserves analytical intelligence from foundation skills that handoff packages compress away.

### Step 9: Build Lead Context Object

Merge all extracted data into a single `lead_context` object.

---

## Output

```yaml
lead_context:
  mechanism:
    name: string
    type: string
    e5_type: string
    uniqueness_claim: string
    credibility_anchors: [object]
  structure:
    campaign_thesis: string
    argument_strategy_type: string
    simple_segue: object
    lead_handoff_data: object
  promise:
    primary_statement: string
    emotional_frame: string
    supporting_promises: [object]
    promise_ceiling: string
  root_cause:
    expression: string
    what_they_think: string
    what_it_really_is: string
    villain_profile: object
  proof:
    overall_strength: float
    knockout_proof: object
    top_credibility_elements: [object]
  headline:
    main_headline: string
    subheadline: string
    implied_promise: string
    mechanism_reference: string
  campaign_brief:
    campaign_thesis: string
    tone_direction: string
    avatar_summary: string
    market_sophistication: integer
    proof_approach: string
    promise_ceiling: string
  context_reservoir:
    loaded: boolean
    voc_quotes: object
    proof_elements_detailed: [object]
    customer_language: object
    fssit_intelligence: object
    expectation_schema: object
    expression_anchoring: object
    arena_intelligence: object
    counter_intuitive_core: object
  loading_status:
    all_required_loaded: boolean
    campaign_brief_loaded: boolean
    context_reservoir_loaded: boolean
    missing: [string]
```

---

## Quality Gates

- [ ] Mechanism package loaded with name and uniqueness_claim
- [ ] Structure package loaded with campaign thesis and lead handoff data
- [ ] Promise package loaded with primary statement
- [ ] Root cause loaded with three_part_structure
- [ ] Proof inventory loaded with credibility elements
- [ ] Headline loaded and parsed
- [ ] Lead context object assembled
- [ ] `all_required_loaded` = true

---

## Handoff

Pass `lead_context` to:
- **0.4-input-validator.md** (for threshold validation)
- Available to all Layer 1+ microskills via shared context
