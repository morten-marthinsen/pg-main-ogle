# 0.1-upstream-loader.md

> **Layer:** 0 (Foundation)
> **Skill:** EC-05-ecomm-assembly
> **Purpose:** Load hero + section copy + micro-scripts + feature package
> **Outputs:** `upstream_context` with all copy packages

---

## FUNCTION

Load ALL upstream copy outputs: hero copy from EC-02, section copy from EC-03, micro-scripts from EC-04, and feature package from EC-01. Verify all expected outputs exist.

---

## LOADING PROTOCOL

```
LOAD from ~outputs/[project-code]/e-comm/:
  1. hero-copy-draft.md (EC-02)
  2. section-copy-package.json (EC-03)
  3. section-copy-assembled.md (EC-03)
  4. micro-scripts.json (EC-04)
  5. feature-package.json (EC-01)

VERIFY each file exists and is non-empty
BUILD combined_context with all outputs accessible
```

---

## OUTPUT STRUCTURE

```json
{
  "upstream_context": {
    "hero_loaded": true,
    "section_copy_loaded": true,
    "micro_scripts_loaded": true,
    "feature_package_loaded": true,
    "all_outputs_present": true
  }
}
```

---

## FAILURE MODES

| Mode | Trigger | Remediation |
|------|---------|-------------|
| CRITICAL | Any upstream output missing | HALT -- execute missing skill first |
| DEGRADED | Micro-scripts missing (EC-04 not run) | Proceed without micro-scripts, note gap |

---

**Skill Status:** COMPLETE
