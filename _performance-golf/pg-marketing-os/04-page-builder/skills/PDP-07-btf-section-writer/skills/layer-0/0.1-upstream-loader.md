# 0.1-upstream-loader.md

> **Layer:** 0 (Foundation)
> **Skill:** PDP-07-section-copy
> **Purpose:** Load strategy map + feature package
> **Outputs:** `upstream_context` object with section map and features

---

## FUNCTION

Load the e-commerce strategy from PDP-01 (section map with priorities and word budgets) and the feature package from PDP-02 (named features with micro-scripts). These two packages provide the complete context for writing every BTF section.

---

## LOADING PROTOCOL

### Step 1: Load Strategy
```
LOCATE: ecomm-strategy.yaml
EXTRACT:
  active_sections: [all BTF sections with priority, word budget, proof type, crossover skill]
  excluded_sections: [with rationale]
  word_budget_summary: [total and per-section]
  crossover_map: [section-to-skill mappings]
```

### Step 2: Load Feature Package
```
LOCATE: feature-package.json from PDP-02
EXTRACT:
  all_features: [with exact names and micro-scripts]
  feature_hierarchy: [hero, supporting, technical]
  naming_validation: [confirmation all names are final]
```

### Step 3: Build Section Writing Context
```
FOR each active BTF section:
  ASSEMBLE:
    section_number, section_name, priority, word_budget
    proof_type_required
    crossover_skill (if any)
    applicable_features (which PDP-02 features belong in this section)
    design_notes_seed (from PDP-01)
```

---

## OUTPUT STRUCTURE

```json
{
  "upstream_context": {
    "btf_sections": [
      {
        "section_number": 9,
        "section_name": "Problem/Solution",
        "priority": 3,
        "word_budget": 250,
        "proof_type": "customer_story",
        "crossover_skill": "13-root-cause",
        "applicable_features": ["Feature X"]
      }
    ],
    "features": { "hero": [], "supporting": [], "technical": [] },
    "total_btf_word_budget": 2950,
    "loading_status": "complete"
  }
}
```

---

## FAILURE MODES

| Mode | Trigger | Remediation |
|------|---------|-------------|
| CRITICAL | PDP-01 strategy not found | HALT -- execute PDP-01 |
| CRITICAL | PDP-02 feature package not found | HALT -- execute PDP-02 |
| DEGRADED | Crossover mappings incomplete | Proceed, write ecom-native |

---

**Skill Status:** COMPLETE
