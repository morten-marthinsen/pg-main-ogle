# 0.1-upstream-loader.md

> **Layer:** 0 (Foundation)
> **Skill:** LP-15-ecomm-assembly
> **Purpose:** Load hero + section copy + micro-scripts + feature package
> **Outputs:** `upstream_context` with all copy packages

---

## FUNCTION

Load ALL upstream copy outputs: hero copy from PDP-03, section copy from PDP-07, micro-scripts from PDP-08, and feature package from PDP-02. Verify all expected outputs exist.

---

## LOADING PROTOCOL

```
LOAD from ~outputs/[project-code]/e-comm/:
  1. hero-copy-draft.md (PDP-03)
  2. section-copy-package.json (PDP-07)
  3. section-copy-assembled.md (PDP-07)
  4. micro-scripts.json (PDP-08)
  5. feature-package.json (PDP-02)

VERIFY each file exists and is non-empty
BUILD combined_context with all outputs accessible
```

---

## OUTPUT STRUCTURE

```json
{
  "upstream_context": {
    "hero_loaded": true,
    "section_copy_loaded": true,
    "micro_scripts_loaded": true,
    "feature_package_loaded": true,
    "all_outputs_present": true
  }
}
```

---

## FAILURE MODES

| Mode | Trigger | Remediation |
|------|---------|-------------|
| CRITICAL | Any upstream output missing | HALT -- execute missing skill first |
| DEGRADED | Micro-scripts missing (PDP-08 not run) | Proceed without micro-scripts, note gap |

---

**Skill Status:** COMPLETE
